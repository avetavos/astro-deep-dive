---
title: 'Markdown & MDX'
description: 'หน้า Markdown กับ MDX, frontmatter, การใส่ layout ให้ content, การฝัง component ใน MDX, pipeline Sätteri ใน Astro 7 และเมื่อไหร่ควรใช้ .md กับ .mdx'
sidebar:
  order: 5
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Markdown เป็นหน้า

วางไฟล์ `.md` ลงใน `src/pages/` แล้วไฟล์นั้นจะกลายเป็น route ที่ render เป็น HTML — file-based routing แบบเดียวกับ `.astro` เป็นวิธีที่เร็วที่สุดในการเขียน content

```md
---
title: Hello World
---

# {frontmatter.title}

This is **Markdown**. Astro renders it to HTML at build time.
```

บนสุดคือ **frontmatter** — บล็อก YAML คั่นด้วย `---` — เก็บ metadata อย่าง `title` หรือ `date` ภายใน body ของ Markdown คุณอ้างถึงค่าพวกนั้นผ่าน object `frontmatter` ได้

## ใส่ layout ให้ Markdown

Markdown ดิบ ๆ render ออกมาเป็น HTML fragment เปล่า ๆ ไม่มี shell มาห่อด้วย chrome ของไซต์ ให้ชี้ key `layout` ใน frontmatter ไปที่ component layout

```md
---
layout: ../../layouts/BaseLayout.astro
title: Hello World
---

# Content here goes into the layout's default slot.
```

layout จะได้รับ frontmatter ของหน้าเป็น props (ผ่าน `Astro.props.frontmatter`) และ Markdown ที่ render แล้วจะเติมลงใน default `<slot />` ของ layout

```astro
---
// src/layouts/BaseLayout.astro
const { frontmatter } = Astro.props;
---
<html>
  <head><title>{frontmatter.title}</title></head>
  <body>
    <article><slot /></article>   <!-- the Markdown body lands here -->
  </body>
</html>
```

## MDX: Markdown บวก component

`.mdx` คือ Markdown ที่มีพลังแบบ JSX ฟีเจอร์เด่น: คุณ **import แล้ว render component** — component Astro หรือ framework — ได้ตรงในร้อยแก้วของคุณ

```mdx
---
title: Release notes
layout: ../../layouts/BaseLayout.astro
---
import Callout from '../../components/Callout.astro';
import Chart from '../../components/Chart.jsx';

# {frontmatter.title}

Regular Markdown paragraphs work as usual.

<Callout title="Heads up">
  You can drop a component into the middle of prose.
</Callout>

And even an interactive island:

<Chart client:visible data={[1, 2, 3]} />
```

MDX เปิดใช้ด้วย integration `@astrojs/mdx` เมื่อติดตั้งแล้ว ไฟล์ `.mdx` เป็นหน้า (หรือ entry ของ collection) ที่ผสม content กับ component ได้อย่างอิสระ — รวมถึง island ที่มี client directive เพราะ MDX ประกอบกับทุกอย่างใน Astro ได้

<Mermaid title="Markdown render เป็น HTML; MDX ฝัง component ได้ด้วย" code={diagram} />

export const diagram = `flowchart LR
  md["Markdown or MDX file"] --> fm["frontmatter provides metadata"]
  fm --> layout["layout key wraps it in a shell"]
  md --> body["body renders to HTML"]
  body --> comp["MDX can also embed Astro and framework components"]
  layout --> out["final page"]
  comp --> out`;

## Astro 7 render Markdown ด้วย Sätteri

ตั้งแต่ Astro 7 ไฟล์ `.md` และ `.mdx` ถูก render ด้วย **Sätteri** ซึ่งเป็น Markdown pipeline แบบ native ที่ขับเคลื่อนด้วย Rust ของ Astro เอง แทน pipeline remark/rehype (unified) ที่เป็น JavaScript สำหรับ content ธรรมดาไม่มีอะไรเปลี่ยนนอกจากความเร็ว — นี่เป็นหนึ่งในเหตุผลที่ build ของ Astro 7 เร็วขึ้น 15–61%

จุดที่มีผลคือ **plugin** ถ้าโปรเจกต์ของคุณพึ่งพา remark หรือ rehype plugin (custom container, reading-time, image transform) พวกนั้นคือ unified plugin และ Sätteri ไม่รันมัน ต้อง opt กลับไปใช้ unified pipeline อย่างชัดเจน:

```js
// astro.config.mjs
import { defineConfig } from 'astro/config';
import { unified } from '@astrojs/markdown-remark';

export default defineConfig({
  markdown: {
    processor: unified(),          // ให้ remark/rehype plugin ทำงานต่อได้
    remarkPlugins: [/* ... */],
    rehypePlugins: [/* ... */],
  },
});
```

`@astrojs/markdown-remark` ตอนนี้เป็น package ที่ต้องติดตั้งแยก กฎง่าย ๆ คือ เริ่มด้วย Sätteri (default) แล้วค่อยสลับ processor เมื่อ plugin ที่คุณต้องใช้จริง ๆ ไม่มีของ native ให้แทน

## `.md` กับ `.mdx`

หยิบ **`.md`** มาใช้เมื่อไฟล์เป็น content ล้วน ๆ — บล็อกโพสต์, หน้า doc, โน้ต `.md` เรียบง่ายกว่าและไม่มี overhead ของ component

หยิบ **`.mdx`** มาใช้เมื่อ content ต้องฝัง component — กล่อง callout, chart, demo ที่ interactive, figure ที่ใช้ซ้ำได้ ต้นทุนคือ integration เพิ่มและการ process ที่หนักขึ้นเล็กน้อย ดังนั้นอย่า default ทุกอย่างเป็น MDX ใช้ MDX เฉพาะตรงที่คุณผสม content กับ component จริง ๆ

## หมายเหตุเรื่อง content collection

การวางไฟล์ Markdown ตรง ๆ ใน `src/pages/` ใช้ได้ แต่ไม่สเกลไปยังบล็อกหรือไซต์ doc ขนาดใหญ่: ไม่มี schema validation, ไม่มี frontmatter ที่มี type, ไม่มีการ query วิธีสมัยใหม่ในการจัดการไฟล์ Markdown/MDX **จำนวนมาก** คือ **content collection** — ชั้น content ที่มี type, validate และ query ได้ — ที่เป็นเนื้อหาทั้งหมดของโมดูลถัดไป หน้าหลวม ๆ โอเคสำหรับไฟล์ไม่กี่ไฟล์; collection คือวิธีรันไซต์ content จริงจัง

<Callout title="สรุปหนึ่งบรรทัด">
ไฟล์ `.md` และ `.mdx` ใน `src/pages/` เป็น route ที่ render เป็น HTML โดยมี **frontmatter** สำหรับ metadata และ key `layout` เพื่อห่อ content ใน shell (body เติมลง `<slot />` ของ layout) **MDX** เพิ่มความสามารถ import แล้ว render component — รวมถึง island — ในร้อยแก้ว ใช้ `.md` สำหรับ content ล้วน ๆ และ `.mdx` เมื่อต้องการ component; และสำหรับไฟล์ **จำนวนมาก** ให้เลื่อนขั้นไปใช้ **content collection** ที่มี type (โมดูลถัดไป) Astro 7 render ทั้งสองแบบด้วย pipeline native ชื่อ **Sätteri**; opt ไปใช้ processor `unified()` จาก `@astrojs/markdown-remark` เฉพาะเมื่อต้องใช้ remark/rehype plugin
</Callout>

export const quizMarkdown = [
  {
    q: 'คุณห่อหน้า Markdown ด้วย shell ของไซต์ยังไง?',
    options: [
      'import layout ใน body ของ Markdown',
      'ตั้งค่า key `layout` ใน frontmatter ให้เป็น component layout; Markdown ที่ render แล้วเติมลง default slot ของตัวเอง',
      'Markdown ใช้ layout ไม่ได้',
      'ห่อหน้านั้นใน `<div>`',
    ],
    answer: 1,
  },
  {
    q: 'ความสามารถที่นิยาม MDX เหนือ Markdown ธรรมดาคืออะไร?',
    options: [
      'render เร็วกว่า',
      'คุณ import แล้ว render component (รวมถึง island) ในร้อยแก้วได้',
      'รองรับ frontmatter',
      'รองรับ heading',
    ],
    answer: 1,
  },
  {
    q: 'เมื่อไหร่ควรใช้ `.md` มากกว่า `.mdx`?',
    options: [
      'เสมอ — .mdx เลิกใช้แล้ว',
      'เมื่อไฟล์เป็น content ล้วน ๆ ไม่มี component — .md เรียบง่ายและเบากว่า',
      'เฉพาะหน้าแรก',
      'ไม่มีวันใช้ — .mdx ดีกว่าเสมอ',
    ],
    answer: 1,
  },
  {
    q: 'วิธีสมัยใหม่ในการจัดการไฟล์ Markdown/MDX จำนวนมากในสเกลใหญ่คืออะไร?',
    options: [
      'วางทั้งหมดใน src/pages/',
      'content collection — ชั้น content ที่มี type, validate และ query ได้ (โมดูลถัดไป)',
      'database',
      'ไฟล์ .mdx ยักษ์ไฟล์เดียว',
    ],
    answer: 1,
  },
  {
    q: 'remark plugin ของคุณหยุดทำงานหลังอัปเกรดเป็น Astro 7 เพราะอะไร และทำยังไง?',
    options: [
      'Astro ไม่รองรับ plugin อีกแล้ว',
      'Astro 7 render Markdown ด้วย pipeline native ชื่อ Sätteri; ติดตั้ง `@astrojs/markdown-remark` แล้วตั้ง `markdown.processor: unified()` เพื่อให้ remark/rehype plugin ทำงานต่อ',
      'เปลี่ยนชื่อไฟล์เป็น .mdx',
      'ย้าย plugin ไปไว้ใน `vite.plugins`',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="components/markdown-and-mdx" questions={quizMarkdown} />

<ProgressTracker client:visible id="components/markdown-and-mdx" />
